Run `docker-compose up -d` to start up the database.

To start the server, run `go run main.go` in the /server directory in one tab.

To start the browser, run `yarn client` in the /client directory 
